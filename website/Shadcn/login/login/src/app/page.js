import NextButton from '@/components/pages/nextButton'
import MainMove from '@/components/Moving/movemain'
import HolderButton from '@/components/pages/HolderButton';

export default function Home() {
  return (
      <div className="flex justify-center items-center">
        {/* <NextButton/> 
        <MainMove/>*/}
        <HolderButton/>
      </div>
  );
}
